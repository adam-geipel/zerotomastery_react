

const CardList = () => {
    return (
            <>
            {/*filteredMonsters.map((monster) => {
                return (
                        <div key={`monster_${monster.id}`}>
                            <h1>{monster.name}</h1>
                        </div>);
            */
            <div>Card List here</div>})
            </>)
}